# First UI Polished

这一版单独保存在 `demo_first_ui_polished/`，用于保留第一次 demo 更强的参考氛围感，并继续打磨。

## 与原 demo 的区别

- 保留第一次版本里更明显的参考视频质感
- 重新处理文字层和视频层的关系
- 新增局部遮罩、扫光、尾注和角标细节
- 保留键盘切换、确认态和准星追踪

## 打开方式

直接打开 `index.html` 即可。

## 快捷键

- `A` / `←`：左侧
- `D` / `→`：右侧
- `Enter` / `Space`：确认
- `Esc`：重置
